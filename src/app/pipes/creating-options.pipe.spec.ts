import { CreatingOptionsPipe } from './creating-options.pipe';

describe('CreatingOptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new CreatingOptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
