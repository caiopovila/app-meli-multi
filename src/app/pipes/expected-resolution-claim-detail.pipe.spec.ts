import { ExpectedResolutionClaimDetailPipe } from './expected-resolution-claim-detail.pipe';

describe('ExpectedResolutionClaimDetailPipe', () => {
  it('create an instance', () => {
    const pipe = new ExpectedResolutionClaimDetailPipe();
    expect(pipe).toBeTruthy();
  });
});
