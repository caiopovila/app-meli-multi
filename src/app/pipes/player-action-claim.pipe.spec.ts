import { PlayerActionClaimPipe } from './player-action-claim.pipe';

describe('PlayerActionClaimPipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerActionClaimPipe();
    expect(pipe).toBeTruthy();
  });
});
