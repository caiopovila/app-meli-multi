import { ReasonClaimPipe } from './reason-claim.pipe';

describe('ReasonClaimPipe', () => {
  it('create an instance', () => {
    const pipe = new ReasonClaimPipe();
    expect(pipe).toBeTruthy();
  });
});
