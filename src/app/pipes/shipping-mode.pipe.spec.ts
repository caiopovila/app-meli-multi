import { ShippingModePipe } from './shipping-mode.pipe';

describe('ShippingModePipe', () => {
  it('create an instance', () => {
    const pipe = new ShippingModePipe();
    expect(pipe).toBeTruthy();
  });
});
