import { ExpectedResolutionClaimPipe } from './expected-resolution-claim.pipe';

describe('ExpectedResolutionClaimPipe', () => {
  it('create an instance', () => {
    const pipe = new ExpectedResolutionClaimPipe();
    expect(pipe).toBeTruthy();
  });
});
