import { QuestionKeyVerifyPipe } from './question-key-verify.pipe';

describe('QuestionKeyVerifyPipe', () => {
  it('create an instance', () => {
    const pipe = new QuestionKeyVerifyPipe();
    expect(pipe).toBeTruthy();
  });
});
