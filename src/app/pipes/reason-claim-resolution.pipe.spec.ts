import { ReasonClaimResolutionPipe } from './reason-claim-resolution.pipe';

describe('ReasonClaimResolutionPipe', () => {
  it('create an instance', () => {
    const pipe = new ReasonClaimResolutionPipe();
    expect(pipe).toBeTruthy();
  });
});
